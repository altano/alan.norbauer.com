<style>
  @import "@/styles/custom-media-queries.css";

  @layer components {
    ul {
      display: flex;
      flex-direction: column;
      list-style-type: none;
      padding: 0;
      margin-block: 0.5rem;
      column-gap: 1rlh;
      row-gap: 1.5rlh;

      @media (--sm-viewport) {
        row-gap: 2rem;
      }

      @media (--md-up-viewport) {
        flex-direction: row;
        flex-wrap: wrap;
        column-gap: 2rem;
        row-gap: 2rem;
      }
    }
  }
</style>

<ul><slot /></ul>
