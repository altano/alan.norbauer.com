---
import SocialIcon from "@/components/social/SocialIcon.astro";
import { Icon } from "astro-icon/components";
import { getUiUID } from "@/utilities/ui-uid";

export type Props = {
  text?: string | undefined;
  size?: number | undefined;
  icon?: false | undefined;
};

const { size = 19, ...otherOptions } = Astro.props;
const id = getUiUID();
---

<SocialIcon {id} {size} {...otherOptions} social="hackernews">
  <Icon size={size} name="fa6-brands:hacker-news" aria-labelledby={id} />
</SocialIcon>
